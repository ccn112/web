/**
 * @x/ui — shadcn/ui-based primitives (navigation menu, dialog/sheet, form, accordion, tabs,
 * carousel, chat widget primitives). Populated in Phase 2 (Design System).
 */
export const UI_PACKAGE = '@x/ui'
