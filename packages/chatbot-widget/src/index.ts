/**
 * @x/chatbot-widget — shared Claude chat widget + prompt chips. Populated in Phase 5.
 * The request/response contract already lives in @x/shared-types/chat.
 */
export type { ChatRequest, ChatResponse, ChatSuggestion } from '@x/shared-types'
export const CHATBOT_WIDGET_PACKAGE = '@x/chatbot-widget'
